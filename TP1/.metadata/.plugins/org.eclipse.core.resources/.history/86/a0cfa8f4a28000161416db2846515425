import javafx.util.*;
import java.util.*;

public class Main {
	
	public static void main(String[] args) {
		
		//TODO : lecture du fichier passé en paramètre et récupération des arguments
		
		int h = 0;
		int l = 0;
		int n = 0;
		int abs,width,minHeight,max,tmp;
		Pair<Integer,Integer>[] tab = new Pair<Integer,Integer>[n +2];
		
		dots.add(new Pair(0,0));
		dots.add(new Pair(l,0));
		
		//TODO : Tri rapide du tableau
		
		Iterator<Pair<Integer,Integer>> it = dots.iterator();
		
		while (it.hasNext()) {
			
			
		}
		
	}
	
}
